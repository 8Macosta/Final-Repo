class Password{
  //The first part of the class.
constructor(){
  this.publickey = publicky;
  this.privatekey = privatekey;
}
validPublicKey(){
  let key = this.publicky;
  if(key.length >= 8 && key.length <= 25){
    return true;
  }
  else{
    return false;
  }
}
   validPrivatekey(){
     let pkey = this.privatekey;
     if(pkey.)
   }
   static makePrivatekey(){
     let key = '';
     let limit = 14;
     for(x=0;x<5;x++){

     }
   }
 }
